name: Build Pipeline

on:
  push:
    branches: [ "akash" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: List files
        run: ls

      - name: Read file
        run: cat app.txt
